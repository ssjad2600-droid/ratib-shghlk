# وضع المحاكي — لا أسرار فيه، ولذلك يُتتبَّع في git (استثناء في .gitignore).
#
# 🔧 التشغيل:
#     نافذة ١:  npm run emulators
#     نافذة ٢:  npm run dev:emulator
#     ثم:       npm run seed:emulator     ← يبذر حساباً تجريبياً وبيانات محل
#
# الحساب التجريبي:  owner@test.local / test1234
#
# ⚠️ لا يؤثّر على البناء إطلاقاً: `import.meta.env.DEV` تصير false في كل بناء
#    إنتاج فتُشجَّر كتلة الوصل كاملةً (مفحوصٌ في hardening.test.ts).
VITE_USE_EMULATORS=1
