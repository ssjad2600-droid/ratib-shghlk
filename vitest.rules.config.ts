import { defineConfig } from 'vitest/config';

/**
 * إعداد اختبارات القواعد وحدها — تُشغَّل عبر `npm run test:rules` داخل المحاكي.
 *
 * منفصل عن `vitest.config.ts` الذي **يستثنيها** من المجموعة الافتراضية: هي بطيئة نسبياً
 * (تُنشئ بيئة وتمسحها بين كل حالة) وتحتاج محاكياً يعمل، فخلطها بالـ٩٥٠ اختباراً السريعة
 * كان سيُبطئ كل تشغيل ويُفشلها عند من لا يملك Java.
 */
export default defineConfig({
  test: {
    include: ['**/firestoreRules.test.ts'],
    // البيئة تُنشأ وتُمسح بين الحالات؛ مهلة أوسع من الافتراضية تكفي أبطأ جهاز
    testTimeout: 20000,
    hookTimeout: 30000,
    // تسلسلي: كل الحالات تتشارك مشروعاً واحداً في المحاكي، والتوازي يجعلها تمسح بيانات بعضها
    fileParallelism: false,
  },
});
