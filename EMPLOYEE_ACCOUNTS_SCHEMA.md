# هيكل بيانات نظام حسابات الموظفين — المرحلة ١ (القواعد فقط)

هذه الوثيقة تصف **الهيكل الجديد للوثائق** وقواعد الوصول المطبّقة في [firestore.rules](firestore.rules).
لم تُكتب أي بيانات فعلية بعد، ولم يُلمس أي كود في `src/`. المنطق التطبيقي (طبقة البيانات،
واجهة الموظف، إدارة الموظفين، فصل التكلفة فعلياً) يأتي في مراحل لاحقة.

---

## المجموعات/الوثائق الجديدة

### 1. `/employeeIndex/{uid}` — فهرس علوي لحسم هوية المستخدم عند الدخول
```jsonc
{
  "ownerUid": "string",   // uid المالك (المحل) الذي يتبع له هذا الموظف
  "disabled": false        // اختياري — true يوقف وصول الموظف فوراً عبر القواعد
}
```
- **الغرض**: عند تسجيل الدخول بـ uid = U، يقرأ التطبيق `/employeeIndex/{U}`:
  موجودة ⇒ U موظف و`ownerUid` معروف؛ غير موجودة ⇒ U مالك (`ownerUid = U`).
- **الوصول**: الموظف يقرأ فهرسه هو فقط (`get`). لا `list`. الإنشاء/التعديل من المالك فقط
  (يُثبت أنه `ownerUid`) مع منع اختطاف موظف مسجَّل لمالك آخر.

### 2. `/users/{owner}/employees/{uid}` — سجل الموظف لدى المالك
```jsonc
{
  "name": "string",        // اسم الموظف
  "email": "string",       // إيميل دخوله (Firebase Auth)
  "addedAt": "ISO string", // تاريخ الإضافة
  "disabled": false         // true = موقوف (تقرؤه دالة isEmployeeOf فتمنع كل وصوله)
}
```
- **الوصول**: المالك فقط (قراءة/كتابة). الموظف لا يقرؤها مباشرة، لكن دالة `isEmployeeOf`
  في القواعد تقرؤها داخلياً عبر `get()` (يتجاوز قواعد القراءة).

### 3. `/users/{owner}/public/info` — معلومات المحل العامة للموظف
```jsonc
{
  "storeName": "string",
  "logoUrl": "string",
  "currency": "IQD | USD",
  "exchangeRate": 1530
}
```
- **الغرض**: يحتاجها الموظف للعرض وتنسيق الأسعار والطباعة، دون قراءة البروفايل الكامل
  (الذي يحوي الترخيص/كود التفعيل/النسخ الاحتياطي/الإعدادات).
- **الوصول**: الموظف قراءة فقط؛ المالك يكتبها (مرآة للحقول العامة من إعداداته).

### 4. `/users/{owner}/product_costs/{id}` — تكلفة الشراء (منفصلة)
```jsonc
{
  "buyPrice": 12000
}
```
- **الغرض**: فصل `buyPrice` عن وثيقة المنتج لتحقيق **إخفاء حقيقي بالقواعد** (Firestore لا يدعم
  تقييد القراءة على مستوى الحقل). المفتاح `id` يطابق معرّف المنتج في `products`.
- **الوصول**: المالك فقط. الموظف لا وصول له إطلاقاً.
- **ملاحظة**: نقل `buyPrice` فعلياً من `products` إلى هنا (النوع + الحفظ + حسابات الأرباح)
  هو عمل **المرحلة اللاحقة**؛ حالياً القاعدة جاهزة فقط.

### 5. `/users/{owner}/customers_public/{id}` — أسماء الزبائن فقط (إسقاط عام)
```jsonc
{
  "name": "string"    // الاسم فقط — لا balance ولا أي حقل مالي
}
```
- **الغرض**: تمكين الموظف من رؤية قائمة أسماء الزبائن لاختيار زبون لفاتورة دين، **دون**
  قراءة المجموعة الرئيسية `customers` التي تحوي `balance`. (نفس سبب فصل التكلفة: لا تقييد
  قراءة على مستوى الحقل.) المفتاح `id` يطابق معرّف الزبون في `customers`.
- **الوصول**: الموظف قراءة + إنشاء (بمفتاح `name` فقط). التعديل/الحذف للمالك.
- **ملاحظة**: مزامنة هذا الإسقاط (عند إنشاء/تعديل اسم زبون من المالك) هي عمل المرحلة اللاحقة.

---

## المجموعات القائمة — ما يتغيّر في الوصول

| المجموعة | المالك | الموظف |
|---|---|---|
| `/users/{owner}` (بروفايل + إعدادات) | كامل | ❌ لا وصول |
| `products` | كامل | قراءة فقط + تحديث `quantity` فقط |
| `product_costs` | كامل | ❌ لا وصول |
| `public/info` | كامل | قراءة فقط |
| `invoices` | كامل | إنشاء (منسوبة له) + قراءة فواتيره فقط؛ لا تعديل/حذف |
| `customers` (الرئيسية، فيها balance) | كامل | إنشاء فقط (balance=0/غائب)؛ لا قراءة/تعديل/حذف |
| `customers_public` | كامل | قراءة + إنشاء (اسم فقط) |
| `debt_payments` | كامل | ❌ لا وصول |
| `financial_transactions` | كامل | ❌ لا وصول |
| `expenses` | كامل | ❌ لا وصول |
| `employees` | كامل | ❌ لا وصول مباشر (تُقرأ داخل القواعد فقط) |
| `/employeeIndex/{uid}` | إنشاء/حذف فهارس موظفيه | `get` لفهرسه هو فقط |
| `/activationCodes` | كما هو سابقاً | كما هو سابقاً |

---

## آلية التطبيق في القواعد (ملخص تقني)

- الوصول **جمعي (OR)**: المالك يُخوَّل عبر القاعدة الشاملة `match /{collection}/{docId}`،
  والموظف يُخوَّل عبر مطابقات خاصة تُضاف فوقها. لا تُوسّع مطابقات الموظف صلاحية المالك،
  ولا تمنح غير الموظف الحقيقي (دالة `isEmployeeOf` صارمة).
- `isEmployeeOf(owner)` = مسجّل دخول **و** `exists` سجلّه تحت employees **و** `disabled != true`.
  تعطيل الموظف (`disabled=true`) يقطع كل وصوله فوراً عبر القواعد بلا حاجة لأي إجراء آخر.
- تحديث المنتج مقيّد بـ `diff().affectedKeys().hasOnly(['quantity'])` ⇒ يسمح بخصم المخزون
  عند البيع ويمنع تعديل السعر/الاسم/الحد أو الحذف.
- قراءة الفواتير مقيّدة بـ `resource.data.createdByUid == request.auth.uid` ⇒ استعلام السرد
  يجب أن يفلتر بـ `where('createdByUid','==',uid)` وإلا رُفض كاملاً.

---

## قيد تقني مهم يجب حسمه في مرحلة المنطق (قبل تفعيل بيع الموظف بالدين)

بما أن الموظف **ممنوع من الكتابة على `balance`** لأي زبون، فإن فاتورة الدين التي ينشئها
تُسجّل الدين على **الفاتورة نفسها** (`remainingAmount` + `customerId`) لكنها **لا تُحدّث
`balance` الزبون تلقائياً**. لذلك رصيد الزبون في قسم الديون لن يعكس ديون الموظف فوراً.
يجب أن تقرّر لاحقاً آلية التسوية (مثلاً: احتساب الرصيد المستحق من الفواتير، أو مزامنة
يجريها المالك)، وهذا **قرار منطق تطبيقي** خارج نطاق قواعد هذه المرحلة.
