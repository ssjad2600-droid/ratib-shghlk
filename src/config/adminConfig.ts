/**
 * uid مالك النظام (مطوّر التطبيق) — يفتح لوحة إدارة أكواد التفعيل.
 *
 * مصدر واحد للحقيقة: كان مكرّراً في AdminPanel و DashboardLayout، فأي تعديل في أحدهما
 * دون الآخر يُظهر/يُخفي الزر بشكل غير متسق. الحماية الفعلية تبقى في قواعد Firestore
 * (activationCodes: list/create مقصورة على هذا الـuid) — هذا الثابت لإخفاء الواجهة فقط.
 */
export const ADMIN_UID = 'YmRTHjZcMmcybuIhNZqFAp7u2H43';

/** رقم التواصل/الدعم الفني للمزوّد — مصدر واحد يظهر في ترويسة البرنامج وشاشة التفعيل. */
export const SUPPORT_PHONE = '07724106414';
/** نفس الرقم بصيغة wa.me الدولية (بلا صفر بادئ، بمقدّمة العراق). */
export const SUPPORT_PHONE_INTL = '9647724106414';
