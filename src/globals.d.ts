/**
 * ثوابت يحقنها Vite عند البناء (`define` في vite.config.ts).
 *
 * تُعرَّف هنا لأن `tsc` لا يعرفها — فهي تُستبدَل نصّياً وقت البناء ولا وجود لها
 * في أي وحدة يُمكن استيرادها.
 */

/** رقم إصدار التطبيق من `package.json` — يقارنه فحص التحديث. */
declare const __APP_VERSION__: string;
